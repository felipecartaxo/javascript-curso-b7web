let number = 0;

while (number < 10) {
    console.log(`The current number is ${number}`);
    number += 1;
}

/* for (let number = 0; number < 10; number += 1) { //  * Este código faz exatamente a mesma coisa que o "while" acima
    console.log(`The current number is ${number}`);
} */