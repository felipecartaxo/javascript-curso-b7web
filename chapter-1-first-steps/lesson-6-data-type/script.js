let first_name = "Felipe"; // * String
let age = 26; // * Número
let connected = true; // * Valor booleano
let food = ["coffee", "chocolate cake", "cookies"]; // * Array
let place = { city: "João Pessoa", state: "PB" }; // * Objeto

console.log(typeof place); // * Imprime o tipo de uma determinada variável
console.log(place.city + ", " + place.state); // * Imprime os atributos do objeto 'place'